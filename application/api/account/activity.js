// {"Message":"Missing required scope.","StatusCode":403,"TraceID":"69ecf08f-358f-4cbd-80ef-033adf79c9b0"}
({
  access: 'public',
  method: async ({ contracts }) => {
    //     const client = await domain.ts.clients.getClient({});
    //     const result = [];
    //     for (const contract of contracts) {
    //       contract.live = contract.live === 1 || contract.live === '1' || contract.live === true || contract.live === 'true';
    //       const endpoint = ['Accounts', contract.account, 'Activity'];
    //       const responce = await lib.ts.send({ method: 'GET', live: contract.live, ver: 'v2', endpoint, token: client.tokens.access });
    //       console.log(responce);
    //     }
    //     return result;
  },
});
