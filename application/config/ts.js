({
  url: {
    protocol: 'https',
    live: 'api.',
    sim: 'sim-api.',
    domen: 'tradestation.com',
  },
  ptfin: {
    pkey: process.env.pkey,
    secret: process.env.secret,
    rtoken: process.env.rtoken,
  },
});
